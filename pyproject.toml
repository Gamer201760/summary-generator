[project]
name = "resume"
version = "0.1.0"
description = "JSONL-driven resume generator (pdf / markdown / text)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fpdf2>=2.8.4",
]

[project.scripts]
resume = "resume:main"
